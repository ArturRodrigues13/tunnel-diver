draw_self(); 

// Um monte de testes pra garantir que o sistema tava funcionando, vou deixar aí, foi divertido

/*

draw_set_color(c_aqua);
draw_set_alpha(.5);

if(descendo) {
	draw_rectangle(x,y + sprite_height + (distancia * dist),x + sprite_width - 1,y + sprite_height + (distancia * dist) + 5,false);
	
	//draw_sprite_ext(spr_crushing,0,x,y + sprite_height,image_xscale,(distancia * dist),0,c_white,.5);
} else {
	
	//draw_sprite_ext(spr_crushing,0,x,y + sprite_height,image_xscale,distancia - (distancia * dist),0,c_white,.5);
	draw_rectangle(x,y_final - (distancia * dist) - 5,x + sprite_width - 1,y_final - (distancia * dist),false);
}

draw_set_color(c_white);
draw_set_alpha(1);
*/